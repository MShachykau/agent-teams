# Dependency Audit Report

## Outdated Packages

### Frontend (`frontend/package.json`)

| Package | Current | Latest | Risk |
|---------|---------|--------|------|
| `react` / `react-dom` | 16.14.0 | 19.x | HIGH — EOL, pinned 4+ years behind |
| `react-scripts` | 4.0.3 | — | HIGH — CRA is deprecated/unmaintained |
| `typescript` | 4.1.2 | 5.x | HIGH — 4+ years old, missing modern features |
| `@types/react` / `@types/react-dom` | (React 16 era) | latest | HIGH — mismatched with current React types |
| `react-redux` | 7.2.9 | 9.x | MEDIUM — two major versions behind |
| `redux` | 4.2.1 | 5.x | MEDIUM — RTK not used at all |
| `styled-components` | 5.3.11 | 6.x | MEDIUM — one major version behind |
| `@types/node` | 12.20.55 | 22.x | MEDIUM — 10 major versions behind |
| Testing / type packages | various | — | LOW — test-only impact |

### BFF (`bff/package.json`)

| Package | Current | Latest | Risk |
|---------|---------|--------|------|
| `express` | 4.18.2 | 5.x | MEDIUM — Express 5 stable released 2024 |

---

## Circular Dependencies

None detected. The import graph is strictly acyclic in both `frontend/` and `bff/`.

---

## Leaked devDependencies

`frontend/package.json` has **no `devDependencies` section** — all packages (including `@testing-library/*`, `@types/*`, `typescript`, `react-scripts`, `web-vitals`) are misclassified under `dependencies`. None of these packages are imported in production source files; this is a structural misclassification inflating the production dependency surface.

`bff/` is clean — `nodemon` is correctly placed in `devDependencies`.

---

*Report generated by dependency-auditor · 2026-03-17*
